fun strings_are_immutable_even_if_interned() {
    var str1 = "hello";
    var str2 = "hello";
    str2 += " world";
    println("str1 is " + str1);
    println("str2 is " + str2);
    assert(str1 != str2, 'strings should be immutable');
}

strings_are_immutable_even_if_interned();